import React from 'react';


const VideoSlider = () => {
    let player = undefined;

    _onReady = (event) => {
        player = event.target;
    };

    return (
        <div></div>
    );
}

export default VideoSlider;